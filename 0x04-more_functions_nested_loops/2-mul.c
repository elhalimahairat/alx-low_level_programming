#include "main.h"
/**
 * mul - a function that return multiplication of two numbers
 * a and b: input
 *a is an integer
 *b is an integer
 * Return: a*b
 */
int mul(int a, int b)
{
int mul;
mul = a * b;
return (mul);
}
