#include "main.h"
/**
 * print_number - a function that prints an integer
 * @n: input
 * Return: inputted integer
 */
void print_number(int n)
{
if (n >=)
_putchar(48 + n);
else
n = -n;
_putchar('-');
_putchar(48 + n);
return;
}
